// Thin route layer: the index route renders the Server Settings screen.
// Screen implementation lives in src/screens to keep routing separate from feature logic.
export { ServerSettingsScreen as default } from '../src/screens/ServerSettingsScreen';
